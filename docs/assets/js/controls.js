/*class Controls {
  constructor(yoda) {
    this.yoda = yoda;
  }

  keyboardEvents() {
    window.addEventListener("keydown", (e) => {
      switch (e.code) {
        case "ArrowLeft":
          if (this.yoda.x > 0) {
            this.yoda.x -= 50;
          }
          break;
        case "ArrowRight":
          if (this.yoda.x + this.yoda.w < 550) {
            this.yoda.x += 50;
          }
          break;
        case "ArrowUp":
          if (this.yoda.y > 0) {
            this.yoda.y -= 35;
          }
          break;
        case "ArrowDown":
          if (this.yoda.y + this.yoda.h < 490) {
            this.yoda.y += 35;
          }
          break;
      }
    });
  }
}
*/
